allprojects {
    group = 'me.tamkungz'
    version = '1.0.4'

    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

subprojects {
    apply plugin: 'java'

    java {
        toolchain { languageVersion = JavaLanguageVersion.of(17) }
    }
}
